"use client";

/**
 * RDS Alert
 *
 * Alert messages and notifications.
 * Re-exports shadcn/ui Alert with RDS naming.
 */

import { Alert, AlertTitle, AlertDescription } from "../primitives/alert";

export {
  Alert as RdsAlert,
  AlertTitle as RdsAlertTitle,
  AlertDescription as RdsAlertDescription,
};
